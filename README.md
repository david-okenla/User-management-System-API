# User-management-System-API
This simple project was built to build an understanding of a Simple Backend System!

The system makes use of the CRUD Operations:
Create User,
Read User,
Update User,
Delete User.

The system would store user data in a database!

I would be using students as examples for this project.
